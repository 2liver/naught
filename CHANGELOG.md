# 变更日志

## 1.0.0（未发布）

1.0 之约：功能齐备后经确认发布（版本号 → 1.0.0 并打正式标签）。

- **显**（`Cmd/Ctrl+T`）：单一模式、零 UI 的琥珀磷光 CRT——缝合怪像素字体（Fusion Pixel 12px 等宽，OFL）、扫描线、辉光、磷粉余晖（渐暗包络+滚动残影）、噪声、玻璃暗角、入场暖机、滚动刷新带；二期三件套已上线：**真衍射**（竖直亮边 R/B ±1px 彩边，光栅物理模型）、**磷粉激发**（新字符短暂增亮回落）、**真高斯辉光**（分离盒式模糊）。与编、阴/阳正交，笔迹与行号跟随磷色。
- **显·块状反相光标**：显模式下光标不再是无年代感的 I 形细线，而是真机时代的整格闪烁块——落点字格满格点亮为炽磷色（比文字更亮更白），格内字符反相暗显（亮度线性映射保 AA），与原生光标同一眨眼节拍、同步休眠；退出显模式无缝还原 I 形。修复期间顺带确认：滚动条"左侧镜像"的真根因是 Qt 6.9 把滚动条搬进了私有容器（`pos()` 恒为 `(0,0)`），快照改用 `mapTo` 换算编辑器坐标。
- **显·光栅调制批次**：栅条软调制（束斑扫过磷粉栅：亮处束斑宽、暗带随束斑变宽；束斑水平偏转让栅相位随内容梯度微移——斜边出摩尔纹）；束斑不对称核（水平扫描方向 6 抽头重、垂直 2 抽头轻——真机的扫描拉长）；余晖改加法模型（光是叠加的：残影偏暖、静态画面微微增亮——磷粉永不完全熄灭）。
- **显·物理拟真批次**：栅网锚定屏幕（磷粉栅/扫描线/暗角改从屏幕空间采样——修复"跟随鼠标的网纹"，真机上它们是固定玻璃结构）；束斑物理（亮度越高束斑越宽，加法溢出）；余晖分通道衰减（红拖尾最长，残影偏暖）；噪声灰尘（hash 细颗粒+稀疏灰尘点）；滚动刷新带（暗带 3 秒扫一周）；入场暖机（GPU 侧计算，暗→亮→过冲→回落）。
- **显·二期三件套回接 GPU 管线**：QRhi 换代后一度只活在自检里的三件套重新上线——**真衍射**（`crt.frag` 内 ±1px 邻像素差分 R/B 彩边，与 `Crt::edgeDiff` 同模型）、**真高斯辉光**（快照 1/4 降采样 + 三轮分离盒式模糊，`Crt::phosphorBloom`，80ms 节流烘焙）、**磷粉激发**（编辑时刻追踪，新字符三圈软边加色增亮、500ms 指数回落）。修复盒式模糊滑动窗口离一错误：边缘钳制少计一像素，窗口在亮块尾缘变负、`uchar()` 回绕成 253 涂满整行。
- **言**（`Cmd/Ctrl+L`）：选中行头尾批量加「」，空行跳过，一步撤销，之后整段保持选中。
- **隔**（`Cmd/Ctrl+F`）：选中的**每一行**上下各补一个空行（逐行隔离、幂等、一步撤销）——多行选择时每行独立成岛，不再只包整块头尾。言/隔列于右键菜单与菜单栏「编」之下，构成批量校对搭档。
- **去重精简**：描边几何统一为 `strokeOutline`（画/擦/显示同一轮廓）；滚动偏移、光标落点、笔刷口径各收敛为单一入口。

## 0.3.14

- 修复：CI 的 macOS 包未钉部署目标，跟随构建机系统版本（15+），在 macOS 13.7 上启动报 `kLSIncompatibleSystemVersionErr`。现应用与 KF6 动态库均钉 `CMAKE_OSX_DEPLOYMENT_TARGET=12.0`，CI 增加 `vtool` 校验（minos == 12.0）锁死回归。

## 0.3.13

计划阶段收官：模块化 → 行号几何 → 性能 → 深度审核。

- **架构**：1900 行单文件拆为五个模块（画布 / 自绘滚动条 / 行号区 / 编辑器 / 入口），依赖单向无环。
- **编模式行号**：数字与文字共用同一 `QTextLayout` 基线（结构对齐，废除光学启发式）；文末幽灵行号不再显示；自检以"数字对数字"像素比对硬性锁定对齐。
- **性能**：新增 `--bench` 基准工具；笔迹绘制与擦除增加包围盒裁剪（大笔量文档滚动更顺）。
- **审核**：新增 `focusOutEvent`——Shift 按住笔刷时 Cmd+Tab 切走不再滞留鼠标抓取与笔迹会话；AddressSanitizer 全套自检零错误；三平台 CI 每个提交强制跑自检。
- **安装**：一条指令安装自动剥除 Gatekeeper 隔离标记，首次启动不再被拦。

## 0.3.12

- 修复：快速拖动擦除改为连续管段（拖快不再留圆点）。
- 修复：Shift 按住笔刷可跨行号区拖动；行号区鼠标坐标换算修正；行号槽宽度随缩放重算（放大溢出）。
- 笔迹与文字统一撤销/重做（`Cmd/Ctrl+Z / Y`，上限 100 步）；消 = `Cmd/Ctrl+Shift+N`。

## 0.3.5 及更早

- 画布层：涂 / 擦（布尔几何橡皮擦，口径=笔刷）/ 消；笔迹存文档坐标、随滚动、随阴/阳变色。
- 编模式：等宽字体 + 行号 + KSyntaxHighlighting 语法高亮（无运行、无保存）。
- 阴 / 阳；字号缩放（按住加速、步长等比）；触控板捏合缩放；笔刷独立缩放（`Cmd+Shift+=/-/0`）。
- 自绘滚动条（命中区恒 18px、闲置淡出）；慢闪休眠光标；Tab = 4 空格。
- 摹 / 空 / 阴 / 阳 / 涂 / 擦 / 消 / 编 全套快捷键与右键菜单；macOS 菜单栏「项」。
