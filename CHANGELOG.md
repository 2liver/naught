# 变更日志

## 1.0.0（未发布）

1.0 之约：功能齐备后经确认发布（版本号 → 1.0.0 并打正式标签）。

- **言**（`Cmd/Ctrl+L`）：选中行头尾批量加「」，空行跳过，一步撤销，之后整段保持选中。
- **隔**（`Cmd/Ctrl+G`）：选中块上下各补一个空行，已是空行则不重复（幂等），一步撤销。言/隔列于右键菜单与菜单栏「编」之下，构成批量校对搭档。
- **去重精简**：描边几何统一为 `strokeOutline`（画/擦/显示同一轮廓）；滚动偏移、光标落点、笔刷口径各收敛为单一入口。

## 0.3.14

- 修复：CI 的 macOS 包未钉部署目标，跟随构建机系统版本（15+），在 macOS 13.7 上启动报 `kLSIncompatibleSystemVersionErr`。现应用与 KF6 动态库均钉 `CMAKE_OSX_DEPLOYMENT_TARGET=12.0`，CI 增加 `vtool` 校验（minos == 12.0）锁死回归。

## 0.3.13

计划阶段收官：模块化 → 行号几何 → 性能 → 深度审核。

- **架构**：1900 行单文件拆为五个模块（画布 / 自绘滚动条 / 行号区 / 编辑器 / 入口），依赖单向无环。
- **编模式行号**：数字与文字共用同一 `QTextLayout` 基线（结构对齐，废除光学启发式）；文末幽灵行号不再显示；自检以"数字对数字"像素比对硬性锁定对齐。
- **性能**：新增 `--bench` 基准工具；笔迹绘制与擦除增加包围盒裁剪（大笔量文档滚动更顺）。
- **审核**：新增 `focusOutEvent`——Shift 按住笔刷时 Cmd+Tab 切走不再滞留鼠标抓取与笔迹会话；AddressSanitizer 全套自检零错误；三平台 CI 每个提交强制跑自检。
- **安装**：一条指令安装自动剥除 Gatekeeper 隔离标记，首次启动不再被拦。

## 0.3.12

- 修复：快速拖动擦除改为连续管段（拖快不再留圆点）。
- 修复：Shift 按住笔刷可跨行号区拖动；行号区鼠标坐标换算修正；行号槽宽度随缩放重算（放大溢出）。
- 笔迹与文字统一撤销/重做（`Cmd/Ctrl+Z / Y`，上限 100 步）；消 = `Cmd/Ctrl+Shift+N`。

## 0.3.5 及更早

- 画布层：涂 / 擦（布尔几何橡皮擦，口径=笔刷）/ 消；笔迹存文档坐标、随滚动、随阴/阳变色。
- 编模式：等宽字体 + 行号 + KSyntaxHighlighting 语法高亮（无运行、无保存）。
- 阴 / 阳；字号缩放（按住加速、步长等比）；触控板捏合缩放；笔刷独立缩放（`Cmd+Shift+=/-/0`）。
- 自绘滚动条（命中区恒 18px、闲置淡出）；慢闪休眠光标；Tab = 4 空格。
- 摹 / 空 / 阴 / 阳 / 涂 / 擦 / 消 / 编 全套快捷键与右键菜单；macOS 菜单栏「项」。
